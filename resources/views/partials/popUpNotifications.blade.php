<div>
    <a class="button ml-5" href="{{url('/self/notifications')}}">Notificações</a>
    <!-- @each('partials.popUpNotification', $notifications, 'notification')
    @if(!$notifications->isEmpty())
    <a href="{{url('/self/notifications')}}">View all</a>
    @endif -->
</div>